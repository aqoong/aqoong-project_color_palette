name: project_color_palette
description: "This package is a tool for collaboration between designers and Flutter developers. csv to color_palette"
version: 2.0.0
homepage: https://github.com/aqoong/project_color_palette
repository: https://github.com/aqoong/project_color_palette
issue_tracker: https://github.com/aqoong/project_color_palette/issues
topics:
  - codegen
  - build-runner
  - design-system
  - color
  - csv

environment:
  # 3.1.0 is the floor flutter_lints 4 requires.
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  csv: ^6.0.0
  build: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.9

